<!-- SPDX-License-Identifier: CC-BY-SA-4.0 -->

# План разработки Lantern

Статус: рабочая карта проекта.

Этот документ сохраняет этапы исходного технического задания и добавляет
условия перехода между ними. Он не задаёт сроки. Следующий этап начинается
только после того, как текущий работает и проходит свои проверки.

Точный состав первой версии находится в [MVP.md](MVP.md). Если дорожная карта и
MVP расходятся, сначала исправляются документы, а не код.

## Как пользоваться дорожной картой

У каждого этапа есть:

- одна основная цель;
- небольшой набор результатов;
- проверка завершения;
- список функций, которые пока нельзя начинать.

Статусы:

- `в работе` - это текущий этап;
- `ожидает` - начинать рано;
- `завершён` - все критерии проверены;
- `отложен` - этап не входит в v0.1.

Дата или процент готовности не заменяют критерии завершения.

## Этап 0 - спецификация

Статус: `завершён`.

### Цель

Определить, что мы строим, от каких угроз защищаемся и где заканчивается v0.1.
На этом этапе нет криптографического, сетевого или интерфейсного кода.

### Уже подготовлено

- `README.md`;
- `VISION.md`;
- `MVP.md`;
- `THREAT_MODEL.md`;
- `PROTOCOL.md`;
- `SECURITY.md`;
- `PRIVACY.md`;
- `LICENSES.md`;
- `ROADMAP.md`;
- `CONTRIBUTING.md`;
- `CODE_OF_CONDUCT.md`;
- полные тексты лицензий в `LICENSES/`.

### Итог

Документы подготовлены, полные тексты лицензий сохранены, локальные ссылки и
лицензионные метки проверены. Смысловая сверка требований выполнена перед
закрывающим коммитом Этапа 0.

### Условие завершения

- цель и границы v0.1 понятны без устного объяснения;
- модель угроз рассматривает Relay, украденную базу, повторы, исчерпание
  ресурсов, неверное время и перестановку;
- Envelope имеет версию, поля, лимиты и состояния;
- криптография отделена от транспорта;
- лицензии и правила для будущих зависимостей записаны;
- документы не обещают аудит или безопасность, которых ещё нет;
- все локальные ссылки работают;
- Git не содержит незакоммиченных изменений.

Следующая работа после закрывающего коммита Этапа 0 - симулятор сети.

## Этап 1 - симулятор сети

Статус: `завершён`.

### Цель

Проверить алгоритмы маршрутизации на воспроизводимой модели до реализации сети
на Rust.

### Части работы

1. Создать минимальный Python-проект с фиксированным seed и `pytest`.
2. Смоделировать узлы, сообщения, время и встречи.
3. Воспроизвести Alice -> Relay -> Bob.
4. Реализовать Direct Delivery.
5. Реализовать Epidemic.
6. Реализовать Spray-and-Wait.
7. Добавить TTL, потерю, дублирование и перестановку.
8. Добавить квоты памяти и ограничения копий.
9. Провести сценарии на десятках и сотнях узлов.
10. Сохранить реальные метрики и сделать вывод о выборе алгоритма.

Симулятор работает с непрозрачным payload заданного размера. В нём нет
настоящей криптографии и сетевых сокетов.

### Выполненная работа

Этап завершён следующими результатами:

- создан Python-проект с фиксированными версиями test-зависимостей;
- добавлены проверяемые модели узла, сообщения и встречи;
- реализованы Direct Delivery, Epidemic и Binary Spray-and-Wait;
- воспроизводится Alice -> Relay -> Bob;
- считаются доставка, задержка, передачи, трафик и пиковое хранение;
- одинаковый seed даёт одинаковый JSON;
- граничные значения и все три политики покрыты тестами;
- TTL уменьшается при хранении и не начинается заново на Relay;
- просроченные копии удаляются с явной причиной;
- hop limit проверяется до передачи и не тратится на бесполезный последний
  переход;
- причины удаления и запрета передачи входят в результат эксперимента;
- бюджет Spray-and-Wait делится между отправителем и получателем без создания
  новых токенов;
- копия с одним токеном ждёт конечного получателя;
- параметры политики сохраняются в результате эксперимента;
- потери и дубликаты воспроизводятся из фиксированного seed;
- потеря не создаёт копию и не расходует токен Spray-and-Wait;
- дубликат учитывается в трафике, но не добавляется в хранилище;
- перестановка контейнеров без давления квоты не меняет итог доставки;
- попытки, потери, дубликаты и полезные передачи считаются отдельно;
- квота числа копий и байтов проверяется отдельно для каждого узла;
- FIFO-вытеснение основано на локальном времени приёма и стабильном
  `message_id`;
- слишком большая для квоты копия отклоняется без изменения очереди;
- вытеснения, отказы и пиковое хранение одного узла входят в результат;
- удалённые по TTL и квоте `message_id` временно сохраняются отдельно;
- tombstone-набор имеет собственный срок и жёсткий предел числа записей;
- активный tombstone блокирует возврат копии без изменения основной очереди;
- истечение и вытеснение tombstone-записей входят в результат эксперимента;
- добавлен ограниченный синтетический сценарий на 4-50 узлах;
- количество сообщений ограничено 100, а число встреч - 10 000;
- начальное кольцо гарантирует участие каждого узла в сценарии;
- остальные пары встреч воспроизводимо создаются из фиксированного seed;
- сообщения и встречи используют разные потоки случайных чисел;
- все три политики запускаются на одном следе событий;
- имя и параметры сценария сохраняются в JSON;
- метрики одного проверочного запуска на 20 узлах записаны рядом с ограничениями
  эксперимента;
- добавлен последовательный пакетный запуск нескольких сценариев и seed;
- одна серия ограничена 10 seed, 4 сценариями и 120 запусками;
- подробные события не сохраняются между отдельными запусками серии;
- для каждого запуска сохраняются компактные метрики;
- для каждой пары сценария и политики считаются сводные метрики;
- базовая серия из 18 запусков проведена на 20 и 50 узлах;
- исходные параметры, отдельные результаты и агрегаты сохранены в JSON;
- добавлена раундовая модель на 4-200 узлах и до 10 000 встреч;
- один узел участвует не больше чем в одной встрече за раунд;
- одинаковое модельное время позволяет сравнивать сети разного размера;
- обслуживание TTL и tombstone выполняется один раз на уникальное время;
- серия на 20, 50, 100 и 200 узлах выполнена без ошибок и с ошибками канала;
- отдельное исследование сравнивает бюджеты 4, 8, 16, 32 и 64;
- всего в четырёх итоговых сериях сохранено 340 запусков;
- Binary Spray-and-Wait выбран для Rust-ядра;
- начальный экспериментальный бюджет копий зафиксирован равным 32;
- решение, альтернативы и ограничения записаны в
  `simulator/ROUTING_DECISION.md`.

### Условие завершения

- один seed даёт одинаковый результат;
- разные алгоритмы запускаются на одном наборе сценариев;
- считаются доставка, задержка, пересылки, трафик и память;
- потеря и перестановка не ломают симуляцию;
- все тесты проходят через `pytest`;
- выбранный для Rust алгоритм подтверждён измерениями;
- ограничения эксперимента записаны рядом с результатами.

Все перечисленные условия выполнены. Маршрутизацию теперь можно переносить в
Rust только в составе отдельного пакета Этапа 2. Графики не являлись критерием
завершения, а синтетические результаты не считаются доказательством поведения
реальной сети.

## Этап 2 - Lantern Core

Статус: `завершён`.

### Цель

Реализовать ограниченное ядро, которое хранит и маршрутизирует непрозрачные
контейнеры без зависимости от конкретной сети.

### Части работы

- Rust workspace;
- типы Envelope и локальной маршрутной записи;
- строгий CBOR-декодер;
- дедупликация;
- TTL и hop limit;
- очередь и правило вытеснения;
- постоянное хранилище;
- журнал безопасных состояний;
- общий интерфейс транспорта;
- unit-, property-based и негативные тесты.

На ранних шагах `protected_payload` содержит только очевидные тестовые байты.
Их нельзя показывать как защищённую пользовательскую переписку.

### Текущий прогресс

Одиннадцать рабочих пакетов завершены:

- workspace содержит независимые `lantern-core`, `lantern-storage`,
  `lantern-diagnostics`, `lantern-time`, `lantern-transport` и `lantern-node`;
- Rust зафиксирован на версии 1.97.1;
- `unsafe` запрещён для собственного кода workspace;
- добавлены логические типы Envelope версии 1;
- проверяются version, TTL, hop limit, priority и размер payload;
- `message_id` и `recipient_hint` имеют фиксированный размер 16 байт;
- `Debug` не выводит полные идентификаторы, hint и payload;
- добавлена отдельная локальная маршрутная запись;
- входящие TTL, hops и copy budget ограничиваются локальными максимумами;
- проверяется переполнение локального срока;
- реализованы разрешённые переходы состояний;
- добавлены unit-, интеграционные и негативные тесты;
- решение о границе crate записано в
  `docs/adr/0001-rust-workspace-and-core-boundary.md`;
- выбрана и точно зафиксирована `minicbor` 2.2.2;
- реализовано детерминированное кодирование Envelope версии 1;
- строгий декодер до разбора отклоняет вход больше 64 KiB;
- декодер принимает только map из семи ключей `0`-`6` в фиксированном порядке;
- отклоняются повторные и неизвестные ключи, неопределённые длины, неверные
  типы, теги, float, вложенные значения и неканоничные заголовки;
- payload копируется только после проверки структуры, размеров и диапазонов;
- ошибки кодека не содержат входные байты;
- минимальный Envelope зафиксирован точным вектором из 49 байт;
- максимальный логический Envelope кодируется в 64 565 байт и укладывается в
  общий лимит;
- конфигурация `minicbor` не добавляет транзитивных зависимостей;
- полный текст `BlueOak-1.0.0` сохранён в `LICENSES/`;
- решение о строгом кодеке записано в
  `docs/adr/0002-strict-cbor-codec.md`;
- добавлен подсчёт канонического размера Envelope без копирования payload;
- очередь ограничена 1000 активными Envelope и 64 MiB канонических
  CBOR-байтов;
- меньшие локальные квоты разрешены, увеличение выше границ v0.1 запрещено;
- активный повтор и действующий tombstone проверяются до вытеснения;
- дубликат не заменяет сохранённый Envelope и не продлевает tombstone;
- перед применением квоты удаляются локально просроченные записи;
- отдельно не помещающийся Envelope отклоняется без очистки очереди;
- реализовано детерминированное FIFO-вытеснение по `first_seen_at` и
  `message_id`;
- открытые, просроченные и вытесненные ID попадают в ограниченное
  tombstone-хранилище;
- tombstone ограничены 2000 записями и сроком до 7 суток;
- очередь возвращает все удаления будущему транзакционному слою;
- решение записано в
  `docs/adr/0003-bounded-queue-and-deduplication.md`;
- добавлен отдельный crate `lantern-storage`, не меняющий границу ядра и
  транспорта;
- выбрана и точно зафиксирована `rusqlite` 0.40.1 с bundled SQLite 3.53.2;
- очередь, локальные маршрутные поля и tombstone сохраняются в SQLite;
- схема использует `STRICT`-таблицы, application ID и явную версию `1`;
- полный снимок заменяется одной транзакцией, ошибка оставляет предыдущий
  снимок доступным;
- восстановленные строки повторно проходят строгие проверки `lantern-core`;
- число строк, сумма CBOR-байтов, размер значения и размер файла ограничены до
  больших выделений;
- неизвестная схема, другой application ID, несовпадающие лимиты, повреждённый
  CBOR и противоречивый маршрут отклоняются;
- на Unix база получает права `0600`, а символические ссылки отклоняются;
- последнее настенное время сохраняется вместе со снимком;
- при обычном перезапуске достигшие срока записи удаляются и превращаются в
  ограниченные tombstone;
- при откате часов все активные записи консервативно истекают, чтобы TTL не
  продлевался;
- решение и альтернативы записаны в
  `docs/adr/0004-sqlite-queue-persistence.md`;
- лицензии 11 новых прямых и транзитивных пакетов зафиксированы в
  `THIRD_PARTY_NOTICES.md`;
- добавлен закрытый словарь кодов событий и общих результатов;
- диагностическое событие не может содержать строку, ID, Envelope, payload,
  путь, адрес или объект внешней ошибки;
- точные размер и длительность сразу заменяются грубыми категориями;
- диагностические записи содержат порядок, но не абсолютное время;
- in-memory журнал по умолчанию ограничен 2048 записями и 64 KiB логических
  данных;
- верхние границы конфигурации - 10 000 записей, 320 000 логических байтов и 7
  суток;
- заполнение вытесняет самые старые записи, а откат часов очищает журнал;
- журнал не создаёт глобальный logger, файл, stdout, stderr или сетевой запрос;
- решение записано в `docs/adr/0005-bounded-safe-diagnostics.md`;
- добавлен независимый `lantern-time` без сторонних зависимостей;
- логическое время выбирается по монотонному интервалу, настенным часам и
  предыдущему результату и не уменьшается внутри процесса;
- откат настенных часов получает отдельный статус и требует консервативно
  завершить активные записи и очистить диагностический журнал;
- ошибки и `Debug` времени не раскрывают точные значения;
- единое чтение времени проверено совместно с очередью и диагностическим
  журналом;
- решение записано в `docs/adr/0006-monotonic-runtime-clock.md`;
- добавлен общий `FrameTransport` без привязки к сокету или сетевому runtime;
- один непрозрачный кадр ограничен 64 KiB до передачи ядру;
- приём и отправка одной сессии раздельно ограничены 1024 кадрами и 64 MiB;
- адаптер получает буфер вызывающего кода и оставшуюся байтовую квоту до чтения;
- нарушение длины, входящей квоты или контракта завершает сессию без повторного
  вызова адаптера;
- транспортные ошибки используют закрытые категории без адреса, кадра и текста
  ошибки ОС;
- транспортная граница интеграционно проверена со строгим декодером
  `lantern-core`;
- решение записано в `docs/adr/0007-bounded-transport-interface.md`;
- добавлен `proptest` 1.11.0 только как dev-зависимость с отключёнными default
  features;
- каждая группа property-тестов использует ChaCha и фиксированный seed;
- произвольные байты проверяют строгий декодер, а созданные Envelope -
  канонический обратный проход;
- генерируемые цепочки операций проверяют квоты очереди, активные ID и
  tombstone;
- скачки системных часов не уменьшают логическое время, а отклонённый откат
  `Instant` не меняет состояние;
- генерируемые длины кадров проверяют квоты приёма и отправки;
- решение записано в `docs/adr/0008-reproducible-property-tests.md`;
- добавлен `lantern-node`, который владеет временем, очередью,
  SQLite-хранилищем и in-memory диагностикой;
- каждая операция, которая обслуживает или меняет состояние узла, использует
  одно логическое время для всех компонентов;
- очередь восстанавливается при запуске, а откат часов завершает
  активные Envelope и очищает диагностику;
- локальный Envelope сохраняется атомарно, а повтор не создаёт вторую запись;
- после ошибки часов, очереди, хранилища или диагностики текущий объект узла
  переходит в `Failed`;
- работающий узел создаёт транспортную сессию только через `BoundedSession`;
- ошибки и `Debug` жизненного цикла не раскрывают путь, ID, payload, точное время
  или точный размер очереди;
- решение записано в `docs/adr/0009-bounded-node-lifecycle.md`;
- добавлен явно включаемый `PersistentDiagnosticJournal` в отдельной
  SQLite-базе;
- файл диагностики ограничен 10 MiB, а число записей, логический размер и срок
  остаются ограничены `JournalLimits`;
- постоянный снимок заменяется одной транзакцией и не меняет память при ошибке
  commit;
- повреждённые коды, сроки, лимиты, application ID и версия схемы отклоняются;
- обычный запуск по-прежнему не создаёт постоянный журнал;
- пустой файл `.lock` не содержит PID, пути или времени и держит эксклюзивную
  блокировку профиля средствами ОС;
- второй процесс отклоняется до чтения часов и базы, а `stop`, ошибка и `Drop`
  освобождают блокировку;
- разные профили не могут одновременно писать в одну базу диагностики;
- конфликтующие пути очереди, SQLite-sidecar, lock и диагностики отклоняются;
- решение записано в
  `docs/adr/0010-persistent-diagnostics-and-profile-lock.md`;
- итоговый аудит повторно проверил порядок ограничений перед выделениями памяти,
  квоты, восстановление, дедупликацию и безопасные ошибки;
- изменение каждого байта канонического тестового Envelope не вызывает панику:
  результат либо отклоняется, либо остаётся каноническим;
- превышение числа строк в очереди и постоянной диагностике отклоняется до
  загрузки записей;
- сквозной тест узла не находит payload, полные ID и путь очереди в байтах базы
  постоянной диагностики;
- рабочие crate не содержат `unsafe`, прямого вывода в stdout или stderr и
  незакрытых `TODO`;
- граф обычных зависимостей сохраняет `lantern-core`, `lantern-time` и
  `lantern-transport` независимыми границами;
- решение и результаты записаны в
  `docs/adr/0011-stage-2-negative-audit.md`;
- проходят 116 unit-тестов и 34 интеграционных Rust-теста.

Постоянная очередь и восстановление времени после перезапуска работают.
Безопасный типизированный журнал работает в памяти, а отдельное постоянное
хранение включается только явно. Единое
неуменьшающееся время процесса реализовано отдельно от очереди и способов
доставки. Общая ограниченная транспортная граница готова, но TCP,
wire framing и сетевой обмен ещё не реализованы. Жизненный цикл узла пока
остаётся библиотечным API. Блокировка профиля готова для обычных процессов, но
не защищает каталог от вредоносного процесса с теми же правами. Файлы баз не
зашифрованы и пока не могут хранить приватные ключи или открытый текст. Итоговый
отрицательный аудит завершён. Он закрывает условия Lantern Core, но не заменяет
fuzzing, криптографические тестовые векторы, сетевые испытания и независимый
аудит.

### Условие завершения

- все ограничения из `PROTOCOL.md` применяются до больших выделений памяти;
- повреждённые данные не вызывают панику;
- очередь не превышает квоту;
- повтор не создаёт новый объект;
- состояние восстанавливается после перезапуска;
- граница будущего криптографического модуля не зависит от интерфейса
  транспорта; после появления модуля это проверяется повторно по фактическому
  графу зависимостей;
- проходят `cargo fmt --check`, `cargo clippy` и `cargo test`;
- все случаи `unsafe` отсутствуют или отдельно обоснованы и проверены.

## Криптографический рубеж

Статус: `завершён`.

Это не отдельная пользовательская функция, а обязательная проверка между
Lantern Core и передачей настоящего сообщения.

Нужно:

- выбрать стандартизованный E2EE-протокол;
- найти поддерживаемую Rust-реализацию;
- проверить лицензию, API, тестовые векторы и историю аудитов;
- определить создание и смену `recipient_hint`;
- проверить офлайн-доставку и ограниченную перестановку;
- написать отдельное архитектурное решение;
- пройти положительные и отрицательные тестовые векторы.

Если подходящей реализации нет, работа останавливается для обсуждения. Нельзя
заменять её собственной криптографической схемой.

Первый обзор завершён 17 июля 2026 года:

- официальный Signal Protocol соответствует сценарию, но его Rust crate не
  выпускается как обычная стабильная зависимость;
- OpenMLS оставлен будущим группам и слишком сложен для личного MVP;
- ближайшим кандидатом стал Olm через `vodozemac 0.10.0`;
- версия `0.10.0` исправляет отказ от неконтрибутивных X25519-ключей и включает
  строгую проверку Ed25519;
- стабильный Olm v1 всё ещё использует 64-битный MAC и хранит не более 40
  пропущенных message key на receiving chain;
- экспериментальный Olm v2 и собственное изменение протокола отклонены;
- обзор записан в `docs/research/CRYPTOGRAPHY_REVIEW.md`;
- решение не подключать зависимость без изолированной проверки записано в
  `docs/adr/0012-defer-e2ee-library-selection.md`.

Для проверки был выделен отдельный compatibility-эксперимент с
`vodozemac 0.10.0`. Он не входит в рабочий путь узла и проверяет изменение
ciphertext, повторы, перестановку, восстановление состояния, дерево
зависимостей и отсутствие секретов в ошибках.

Compatibility-эксперимент завершён 17 июля 2026 года. Восемь тестов подтвердили
асинхронный обмен, отказ при изменении каждого байта, отклонение повтора,
обратную перестановку 32 сообщений, предел в 40 пропущенных ключей и
восстановление зашифрованного состояния. RustSec не нашёл известных
уязвимостей в отдельном lock-файле. Проверка также обнаружила, что
`InboundCreationResult` раскрывает первый plaintext через `Debug`, поэтому этот
объект нельзя логировать. Код и полный отчёт находятся в
`experiments/vodozemac-compat/`.

Архитектурное решение принято 17 июля 2026 года. `vodozemac 0.10.0` и
стабильный Olm v1 выбраны только для `Lantern v0.1 experimental preview`.
Решение не разрешает сразу передавать настоящий текст. Сначала нужны
версионированный contact bundle, общий лимит неудачных расшифрований, безопасная
граница логов и зашифрованное хранение ratchet state. Условия и риски записаны
в `docs/adr/0013-use-vodozemac-for-experimental-mvp.md`.

Формат двух контактных QR, очная SAS-проверка, одноразовое приглашение и
ограниченная смена `recipient_hint` зафиксированы 17 июля 2026 года в
`CONTACTS.md` и
`docs/adr/0014-two-qr-contact-verification-and-hint-rotation.md`. Ключевое
решение - только invite содержит Olm one-time key, поэтому Bob создаёт одну
двустороннюю сессию после взаимной проверки. Код сессии пока не добавлен.

Внутренний формат защищённого сообщения зафиксирован 17 июля 2026 года в
`PAYLOAD.md` и
`docs/adr/0015-versioned-olm-payload-and-transactional-ratchet.md`. Выбраны
бинарная оболочка `OlmMessage::to_parts()`, семь строгих внутренних типов,
предел защищённого payload 8 KiB и кандидатное состояние Account или Session
до полной проверки Envelope. Реализация и тестовые векторы пока не добавлены.

Постоянный лимит неудачных decrypt зафиксирован 17 июля 2026 года в
`CRYPTO_LIMITS.md` и `docs/adr/0016-persistent-crypto-attempt-limits.md`.
Выбраны связанные profile, contact и pending-contact token bucket,
резервирование до decrypt и отсутствие offline refill после перезапуска.
Limiter пока не реализован и зависит от ещё не написанного секретного
хранилища.

Архитектура секретного хранилища зафиксирована 17 июля 2026 года в
`SECRET_STORAGE.md` и
`docs/adr/0017-sqlcipher-secret-storage-with-argon2id.md`. Выбраны отдельная
SQLCipher-база, Argon2id с профилем RFC 9106, terminal passphrase,
transactional pending outbox и постоянный marker начатой decrypt. Системный
keyring не является обязательным. На момент принятия решения кода хранилища ещё
не было.

Первый кодовый пакет хранилища завершён 17 июля 2026 года. Новый
`lantern-secret-storage` создаёт и строго читает `secrets.kdf`, получает соль из
генератора ОС, проверяет парольную фразу и выводит закрытый 32-байтный ключ через
Argon2id. Рабочая память KDF очищается, а Unix-файл ограничен 128 байтами,
правами `0600`, одним hard link и запретом symlink. В этом первом пакете
SQLCipher ещё не был подключён: опубликованный `libsqlite3-sys` содержал 4.14.0,
тогда как upstream выпустил 4.17.0. Поэтому следующим шагом стал отдельный
compatibility-пакет для актуальной версии.

Криптографический рубеж закрыт 17 июля 2026 года:

- compatibility-пакет SQLCipher подтверждает точную версию 4.17.0, raw key,
  отказ неверному ключу и невозможность изменить схему с неверным ключом;
- официальный `libsqlite3-sys` зафиксирован точным Git rev до выхода нового
  crates.io-релиза с тем же обновлением;
- `lantern-secret-storage` создаёт профиль `0700`, KDF-файл `0600` и
  SQLCipher-базу со строгой схемой и квотами;
- Account и Session pickle дополнительно шифруются штатным API `vodozemac`;
- profile, contact и pending-contact token bucket сохраняются до decrypt и не
  получают offline refill после перезапуска;
- attempt marker, кандидатный ratchet, история и pending outbox связаны
  транзакциями;
- новый `lantern-crypto` не зависит от транспорта и реализует два подписанных
  QR, SAS, два подтверждения контакта, Olm-оболочку и семь внутренних типов;
- точные vectors покрывают все внутренние типы и обе Olm-оболочки;
- подмена внешнего Envelope при неизменном ciphertext отбрасывает кандидатный
  Account или Session, после чего исходный Envelope остаётся принимаемым;
- рабочий chat-путь проверен через две SQLCipher-базы, постоянный outbox и
  защиту от повторного показа;
- обратная перестановка 32 сообщений проходит, а известный предел 40
  пропущенных ключей остаётся явно записанным ограничением Olm v1;
- строгие декодеры дополнительно проходят воспроизводимые property-тесты;
- RustSec не нашёл известных уязвимостей в основном и двух
  compatibility lock-файлах на дату закрытия рубежа;
- полный workspace проходит `cargo fmt`, `cargo clippy` и `cargo test`.

Две несовместимости ранней спецификации с публичным SAS API исправлены в
`CONTACTS.md`, ADR 0014 и ADR 0018. Бинарные bundle связываются с SAS через
однозначный Base64URL-контекст, а незавершённая ручная проверка отменяется при
перезапуске, потому что приватный `Sas` не сериализуется. Ключевые цели проекта
от этого не меняются.

Рубеж не является аудитом и не разрешает называть Lantern безопасным.
Coverage-guided fuzzing, длительные fault-injection испытания и независимая
проверка остаются условиями будущей beta, а не Этапа 3.

## Этап 3 - два устройства в LAN

Статус: `завершён` 17 июля 2026 года.

### Цель

Надёжно передать один Envelope между двумя Linux-устройствами в локальной сети.

### Части работы

- ограниченное обнаружение или ручной адрес;
- соединение между соседями;
- согласование версии;
- ограниченный обмен списком контейнеров;
- запрос недостающего Envelope;
- передача с проверкой длины;
- атомарное сохранение;
- повторное подключение после обрыва;
- безопасная диагностика.

Сначала транспорт проверяется на искусственном непрозрачном payload. Настоящий
текст разрешается только после прохождения криптографического рубежа.

Первый сетевой пакет завершён 17 июля 2026 года:

- добавлен отдельный `lantern-lan`, не меняющий транспортную и
  криптографическую границы;
- listener и исходящее соединение принимают только ручной числовой адрес из
  локальных диапазонов;
- DNS, wildcard, multicast, публичный адрес и нулевой порт соседа отклоняются;
- обе стороны обмениваются фиксированным 8-байтным `Hello` версии 1;
- неизвестная версия, повреждённое поле, закрытие и таймаут останавливают
  соединение до прикладных данных;
- ошибки и `Debug` не содержат адрес соседа и строки ошибки ОС;
- временный probe успешно проверен двумя отдельными процессами через loopback;
- формат и альтернативы записаны в `PROTOCOL.md` и ADR 0019.

В этом пакете нет передачи Envelope, wire framing, списка контейнеров,
повторного подключения или обнаружения соседей. Поэтому Этап 3 остаётся в
работе.

Второй сетевой пакет завершён 17 июля 2026 года:

- после `Hello` добавлен 4-байтный big-endian prefix длины;
- тело ограничено диапазоном 1-65 536 байт;
- `LanConnection` реализует общий `FrameTransport`;
- недоверенная длина проверяется до чтения тела и без выделения памяти;
- один монотонный deadline покрывает prefix и всё тело, включая медленную
  фрагментированную передачу;
- нулевая, завышенная и не помещающаяся длина завершает соединение;
- закрытие на границе кадров отделено от обрыва посередине prefix или тела;
- `BoundedSession` ограничивает число и сумму тел в обоих направлениях;
- точный максимальный кадр 64 KiB передан через публичный API в loopback-тесте;
- probe проверен двумя процессами с синтетическим кадром и ответом;
- wire format, альтернативы и риски записаны в `PROTOCOL.md` и ADR 0020.

Третий сетевой пакет завершён 17 июля 2026 года:

- добавлен отдельный `lantern-sync`, не зависящий от TCP, базы и
  криптографического ядра;
- один однонаправленный batch использует последовательность Offer, Request,
  Transfer и Done;
- Offer содержит не больше 32 отсортированных уникальных `message_id`;
- Request может быть только подмножеством текущего Offer;
- Transfer принимаются строго в порядке Request, а повторённый ID сверяется с
  каноническим Envelope;
- лишние, повторные, неотсортированные и усечённые данные отклоняются;
- входящий кадр разбирается в фиксированном буфере, одновременно декодируется
  не больше одного Envelope;
- успешный loopback-тест передаёт один синтетический Envelope через публичные
  `lantern-lan` и `lantern-transport` API;
- property-тест с фиксированным seed проверяет случайные входные кадры;
- wire format, альтернативы и риски записаны в `PROTOCOL.md` и ADR 0021.

На границе третьего пакета очередь и SQLite ещё не были подключены: полученный
Envelope оставался в тестовом sink памяти. Это ограничение закрыто следующим
пакетом.

Четвёртый сетевой пакет завершён 17 июля 2026 года:

- Transfer дополнен ограниченным route grant из `remaining_ttl`, `hops_taken`
  и `copies_left`;
- grant остаётся вне Envelope и проверяется против его TTL и max hops;
- `NodeRuntime` исключает активный ID и tombstone из Request;
- каждый полностью проверенный Transfer получает локальное время и маршрутную
  запись;
- Envelope и маршрут сохраняются SQLite-транзакцией до чтения следующего
  Transfer;
- обрыв или malformed Transfer не создаёт частично сохранённый объект;
- локальная квота отклоняет слишком большой для настройки объект;
- интеграционный тест восстанавливает принятый Envelope и его маршрут после
  перезапуска;
- решение и риски записаны в ADR 0022.

На границе четвёртого пакета приём и постоянная дедупликация уже работали, но
sender ещё не был подключён к очереди. Это ограничение закрыто следующим
пакетом.

Пятый сетевой пакет завершён 17 июля 2026 года:

- `lantern-core` вычисляет forwarding reservation по локальному deadline;
- hop count увеличивается до Transfer, а бюджет делится по Binary
  Spray-and-Wait;
- уменьшенный copy budget sender сохраняется до сетевой отправки;
- Offer не расходует токены, reservation выполняется только после Request;
- потеря Transfer может сжечь токены, но не создаёт дополнительные копии;
- записи Wait-фазы с одним токеном не предлагаются неизвестному соседу;
- memory cursor вращает окно из 32 ID и достигает записей за первым batch;
- два `NodeRuntime` через настоящий LAN делят 32 токена на 16 и 16;
- обе стороны восстанавливают свои бюджеты из SQLite после перезапуска;
- решение, альтернативы и отказ доступности записаны в ADR 0023.

Шестой и итоговый сетевой пакет завершён 17 июля 2026 года:

- `EncounterRole` задаёт один порядок без взаимной блокировки: initiator сначала
  отправляет, responder сначала принимает, затем направления меняются;
- оба направления используют одну `BoundedSession` и её отдельные квоты приёма
  и отправки;
- обрыв завершает текущую сессию, а повторная попытка явно создаёт новую;
- постоянная дедупликация исключает уже принятый ID после reconnect;
- listener допускает одну активную сессию с IP, не больше 8 попыток за 60
  секунд и хранит не больше 256 ограниченных записей peer;
- IP, `message_id`, payload и системные строки ошибок не записываются в
  диагностику;
- успех и отказ sync представлены только закрытым кодом, общей категорией и
  ограниченным счётчиком;
- настоящий loopback-тест передаёт разные Envelope в обе стороны за одно
  соединение;
- отдельный тест обрывает передачу после Request, подключается снова, сохраняет
  Envelope и проверяет дедупликацию на третьем соединении;
- решение и ограничения записаны в ADR 0024.

Все условия завершения ниже проверены автоматическими тестами. Этап 3 закрыт,
но это не является аудитом безопасности или проверкой реальной сети.

### Условие завершения

- два процесса обмениваются контейнером без интернета;
- обрыв не создаёт частично сохранённый объект;
- дубликат не добавляется заново;
- неизвестная версия и превышение размера отклоняются;
- потери и повторное подключение покрыты интеграционными тестами;
- транспорт не получает открытый текст и ключи.

## Этап 4 - три устройства

Статус: `в работе` с 18 июля 2026 года.

### Цель

Воспроизвести главный сценарий Alice -> Relay -> Bob без прямой связи Alice и
Bob.

### Части работы

- создание и проверка локальных идентичностей;
- внеполосное добавление контакта;
- защищённое короткое сообщение;
- временное хранение на Relay;
- перенос после разрыва связи с Alice;
- проверка и открытие только на Bob;
- истечение, вытеснение и дедупликация;
- перезапуск каждого узла между встречами;
- воспроизводимая инструкция демонстрации.

### План пакетов

1. Постоянный маршрут синтетического Envelope через перезапущенный Relay.
2. Явная граница между crypto outbox, транспортной очередью и входящим
   Envelope.
3. Постоянные профили и проверенные контакты Alice и Bob без секретов на Relay.
4. Создание защищённого сообщения Alice и транзакционное открытие только Bob.
5. Wait-фаза, изменение защищённых полей, replay и повторный показ.
6. Перезапуски, TTL, квоты, вытеснение и аварии между транзакциями.
7. Linux CLI, воспроизводимая демонстрация и итоговая проверка 15 критериев MVP.

Это рабочее разбиение этапа, а не новые функции. Если один пакет окажется
слишком большим, он будет разделён без изменения границ v0.1.

Первый пакет завершён 18 июля 2026 года:

- добавлен настоящий loopback-сценарий Alice -> Relay -> Bob без соединения
  Alice с Bob;
- Alice и Relay разрывают соединение до встречи Relay с Bob;
- Relay завершается и восстанавливает Envelope и route metadata из SQLite;
- после второй встречи Bob сохраняет тот же неизменяемый Envelope;
- третья встреча Relay с Bob не передаёт известный ID повторно и не расходует
  copy budget;
- бюджеты Alice, Relay и Bob после перезапуска в сумме остаются равны 32;
- второй тест проверяет, что Relay с исчерпанным hop limit не предлагает
  Envelope Bob;
- payload остаётся очевидным синтетическим набором байтов, ключи и настоящий
  текст в сетевой тест не добавлены;
- решение и ограничения записаны в ADR 0025.

Второй пакет завершён 18 июля 2026 года:

- добавлен отдельный `lantern-bridge` без сетевого транспорта;
- outbox переносит только ранее сохранённый канонический Envelope и
  подтверждается после записи открытой очереди;
- повтор после аварии сравнивает полный Envelope и не запускает encrypt заново;
- одинаковый ID с другим Envelope останавливает перенос и сохраняет outbox;
- неизвестный `recipient_hint` не запускает decrypt и оставляет пакет для
  дальнейшей передачи;
- входящий Envelope удаляется только после commit ratchet и истории;
- повтор после commit истории завершает очистку очереди без второго показа
  текста;
- оба окна аварии проверены с перезапуском `NodeRuntime`;
- ограничения и остаточные риски записаны в ADR 0026.

Третий пакет завершён 18 июля 2026 года:

- Alice и Bob создают разные постоянные SQLCipher-профили;
- оба подписанных QR проходят через настоящий encoder и строгий decoder;
- обе стороны получают одинаковый SAS и проверяют два Olm-подтверждения;
- изменённый Account Alice, контакт и Session сохраняются одной транзакцией;
- ошибка добавления контакта откатывает изменение Account;
- после повторного открытия сохраняются profile ID, identity key, контактные
  ключи, встречные hints и общий Session ID;
- Relay запускается только с открытой очередью, без секретного профиля;
- ограничения и остаточные риски записаны в ADR 0027.

После третьего пакета остаётся четыре пакета. Следующий пакет должен создать
защищённое короткое сообщение в постоянном профиле Alice, передать его через
границу очереди и открыть только постоянным профилем Bob.

### Условие завершения v0.1

- выполнены все 15 критериев из `MVP.md`;
- Relay не имеет API и ключей для расшифрования;
- изменение защищённого поля обнаруживается;
- повтор показывается не более одного раза;
- квоты соблюдаются при вредоносном вводе;
- тесты и результаты привязаны к хешу коммита;
- версия называется только `experimental preview`.

После этого можно заморозить v0.1. Следующие этапы не входят в её состав.

## Этап 5 - Android

Статус: `отложен`.

Android-клиент, Keystore, фоновые ограничения, Wi-Fi Direct, BLE и уведомления.
Перед началом потребуется отдельная мобильная модель угроз и проверка разрешений
платформы.

## Этап 6 - группы

Статус: `отложен`.

Группы через MLS, изменение состава, эпохи ключей, удаление участника и
восстановление после долгого офлайна. Простая отправка одного сообщения каждому
участнику не считается заменой MLS.

## Этап 7 - интернет-реле

Статус: `отложен`.

Самостоятельно разворачиваемый сервер, непрозрачные mailboxes, квоты и защита от
спама. Сервер не получает ключи переписки. Его добавление потребует отдельной
политики хранения и эксплуатации.

## Этап 8 - карта и кризисные карточки

Статус: `отложен`.

Офлайн-карта, срок актуальности, ограничение точности координат, подписи и
подтверждения источника. Lantern не будет называть обычное пользовательское
сообщение официальным предупреждением.

## Этап 9 - мост Meshtastic

Статус: `отложен`.

USB или BLE-соединение с готовым устройством, короткие контейнеры, очередь LoRa
и соблюдение региональных ограничений. Собственная радиопрошивка не является
целью этапа.

## Этап 10 - аудит и beta

Статус: `отложен`.

Fuzzing всех внешних форматов, анализ зависимостей, статический анализ,
независимый аудит, закрытое тестирование и исправление найденных проблем.

Название `security beta` разрешается только после выполнения условий из
`SECURITY.md`. Аудит не гарантирует отсутствие неизвестных ошибок.

## Что не планируется в текущей дорожной карте

- голосовые и видеозвонки;
- iOS;
- публичная социальная сеть;
- криптовалюта;
- глобальная инфраструктура на миллионы пользователей;
- собственный криптографический алгоритм;
- обещание полной анонимности;
- автоматическая модерация нейросетью.

Добавление одного из этих пунктов требует обсуждения и изменения `VISION.md` и
`MVP.md`. Оно не может появиться случайно во время реализации другого этапа.

## Правило изменения плана

Если эксперимент показывает, что решение не работает, мы не скрываем результат
и не переписываем весь проект сразу. Сначала записываем:

1. проверенный сценарий;
2. фактический результат;
3. причину, если она известна;
4. минимальное исправление;
5. влияние на модель угроз и следующие этапы.

После этого обновляются документы, тесты и только затем код.
